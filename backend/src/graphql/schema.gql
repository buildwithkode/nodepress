# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type BulkResult {
  affected: Int!
}

type ContentTypeModel {
  allowedMethods: JSON
  createdAt: DateTime!
  id: Int!
  name: String!
  schema: JSON!
  updatedAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type DeleteResult {
  message: String!
}

type EntryModel {
  createdAt: DateTime!
  data: JSON
  deletedAt: DateTime
  id: Int!
  locale: String!
  publicId: ID!
  seo: JSON
  slug: String!
  status: String!
  updatedAt: DateTime!
}

type EntryPage {
  data: [EntryModel!]!
  limit: Int!
  page: Int!
  total: Int!
  totalPages: Int!
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON

type MediaModel {
  createdAt: DateTime!
  filename: String!
  height: Int
  id: Int!
  mimetype: String!
  originalName: String!
  size: Int!
  uploadedBy: Int
  url: String!
  webpUrl: String
  width: Int
}

type MediaPage {
  data: [MediaModel!]!
  limit: Int!
  page: Int!
  total: Int!
  totalPages: Int!
}

type Mutation {
  """Archive multiple entries."""
  bulkArchiveEntries(ids: [Int!]!): BulkResult!

  """Soft-delete multiple entries."""
  bulkDeleteEntries(ids: [Int!]!): BulkResult!

  """Publish multiple entries."""
  bulkPublishEntries(ids: [Int!]!): BulkResult!

  """Set multiple entries to pending_review."""
  bulkSetPendingReviewEntries(ids: [Int!]!): BulkResult!

  """Create a new content type. Admin only."""
  createContentType(
    """Restrict allowed API methods"""
    allowedMethods: [String!]
    name: String!

    """JSON array of field definitions"""
    schema: String!
  ): ContentTypeModel!

  """Create a new entry. Requires editor or admin role."""
  createEntry(
    contentTypeId: Int!

    """JSON string of entry field values"""
    data: String!
    locale: String = "en"
    slug: String!
    status: String = "draft"
  ): EntryModel!

  """Create a new webhook. Admin only."""
  createWebhook(
    enabled: Boolean = true

    """Event names, e.g. ["entry.created"] or ["*"]"""
    events: [String!]!
    name: String!

    """HMAC signing secret"""
    secret: String
    url: String!
  ): WebhookModel!

  """Delete a content type and all its entries. Admin only."""
  deleteContentType(id: Int!): ContentTypeModel!

  """Soft-delete an entry (recoverable). Requires editor or admin."""
  deleteEntry(id: Int!): DeleteResult!

  """Delete a media file by filename (admin/editor only)"""
  deleteMedia(filename: String!): DeleteResult!

  """Delete a webhook. Admin only."""
  deleteWebhook(id: Int!): DeleteResult!

  """Send a test ping to a webhook. Admin only."""
  pingWebhook(id: Int!): DeleteResult!

  """Permanently delete a soft-deleted entry. Admin only."""
  purgeEntry(id: Int!): DeleteResult!

  """Restore a soft-deleted entry."""
  restoreEntry(id: Int!): EntryModel!

  """Restore an entry to a previous version."""
  restoreEntryVersion(entryId: Int!, versionId: Int!): EntryModel!

  """Enable or disable a webhook. Admin only."""
  toggleWebhook(enabled: Boolean!, id: Int!): WebhookModel!

  """Update a content type schema or name. Admin only."""
  updateContentType(
    allowedMethods: [String!]
    id: Int!
    name: String

    """JSON array of field definitions"""
    schema: String
  ): ContentTypeModel!

  """Update an existing entry."""
  updateEntry(
    """JSON string of updated field values"""
    data: String
    id: Int!
    slug: String
    status: String
  ): EntryModel!
}

type Query {
  """Fetch a single content type by ID."""
  contentType(id: Int!): ContentTypeModel

  """List all content types."""
  contentTypes: [ContentTypeModel!]!

  """List entries. Authenticated: all statuses. Public: published only."""
  entries(contentTypeId: Int, limit: Int = 20, locale: String, page: Int = 1, search: String, status: String): EntryPage!

  """Fetch a single entry by ID."""
  entry(id: Int!, populate: [String!]): EntryModel

  """List all uploaded media files (authenticated)"""
  mediaFiles(limit: Int = 50, page: Int = 1): MediaPage!

  """Get a webhook by ID. Admin only."""
  webhook(id: Int!): WebhookModel

  """Delivery log for a webhook. Admin only."""
  webhookDeliveries(limit: Int = 50, webhookId: Int!): [WebhookDeliveryModel!]!

  """List all webhooks. Admin only."""
  webhooks: [WebhookModel!]!
}

type WebhookDeliveryModel {
  attempts: Int!
  createdAt: DateTime!
  errorMessage: String
  event: String!
  id: Int!
  payload: JSON!
  responseStatus: Int
  status: String!
  webhookId: Int!
}

type WebhookModel {
  createdAt: DateTime!
  enabled: Boolean!
  events: JSON!
  id: Int!
  name: String!
  updatedAt: DateTime!
  url: String!
}